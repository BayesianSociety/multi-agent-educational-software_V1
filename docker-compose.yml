version: '3.9'
services:
  backend:
    image: node:20-alpine
    working_dir: /app
    command: ["sh", "-lc", "echo backend placeholder && tail -f /dev/null"]
    volumes:
      - ./backend:/app
    ports:
      - "4000:4000"
