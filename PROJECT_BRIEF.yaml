{
  "project_type": "scratch_like_game",
  "backend_required": true,
  "tests": {
    "command_source": "profile",
    "commands": [
      "docker compose up -d",
      "docker compose ps"
    ]
  }
}
